import React from 'react';
import ReactDOM from 'react-dom'; //versao do reacg que lida com a DOM, a arvore de elementos da WEB

import App from './App';


ReactDOM.render(<App />, document.getElementById('root')); //renderiza o arquivo "App" na div "root" no index.html, executar uma unica vez

